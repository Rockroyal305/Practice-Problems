#include <iostream>
#include <fstream>
#include <cstring>
#include <string>

using namespace std;

int main()

{
    char frame[70][5];
    char word_letters[15];
    string word;
    cin >> word;
    for(int i = 0; i < word.length(); i++)
    {
        word_letters[i] = word[i];
    }
    for(int i = 0; i < word.length()* 4 + 1)
    {

    }
}
